#include <stdio.h>
int main (){
   int a,b,c,r;
   int x=2*r;
   scanf ("%d%d%d%d",&a,&b,&c,&x);
   if (x<=a && x<=b && x<=c){
    printf ("ball will fit in the box");
   }
 else {
    printf ("ball will not fit in the box");
 }


    }
